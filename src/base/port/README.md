# base/port

This directory provides fallback and port implementations of utility libraries
for platforms and build configurations where standard libraries are unavailable.

The libraries are wrappers among the standard library and ported implementation.

Once the standard library is available for all platforms, this port
implementation should be removed.
